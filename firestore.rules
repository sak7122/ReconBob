rules_version = '2';
// Local emulator only — permissive. Production rules are defined separately at deploy time.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
